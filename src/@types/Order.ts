export interface Order {
  condition: string,
  created: string;
  customer: string;
  id: string;
  origin_address: string;
  photo: string;
  shipping_address: string;
  size: string;
  sku: string;
  status: string;
  type: string;
}
